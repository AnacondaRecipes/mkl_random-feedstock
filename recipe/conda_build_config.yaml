# Until AnacondaRecipes/aggregate#534 merges: t-only overlay.
# numpy 2.5 matches numpy-base 2.5.2 on staging (host: numpy-base {{ numpy }}).
python:
  - "3.14.* *_cp314t"
numpy:
  - 2.5
zip_keys:
  -
    - python
    - numpy
